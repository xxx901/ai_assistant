[project]
name = "ai-assistant"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "许浩然", email = "16495322+x-hr-git@user.noreply.gitee.com" }
]
requires-python = ">=3.12"
dependencies = [
    "lancedb>=0.38.0",
    "memory>=1.0.0",
    "requests>=2.34.2",
    "sentence-transformers>=6.0.1",
]

[project.scripts]
ai-assistant = "ai_assistant:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = false
